# Reference

`Eruja Hi-Fi (standalone) (1).html` is the Claude Design "bundler" storyboard export —
the canonical source of truth for tokens, class names, SVG art, and API data shapes.
It is NOT part of the build (excluded from tsconfig, eslint, prettier). Kept for reference only.
