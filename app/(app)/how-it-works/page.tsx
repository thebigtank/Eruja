import { ScreenPlaceholder } from '@/components/app/ScreenPlaceholder';

// Deferred static stub (desktop nav item only) — no data read, no content yet.
export default function HowItWorksPage() {
  return <ScreenPlaceholder stage="deferred static stub" title="How it works" />;
}
